#include <cs50.h>
#include <stdio.h>



int main(void)
{
    // play with arrays

    string s = "HI!"; // It uses 4 bytes because of stop sign

    printf("%i %i %i %i \n", s[0], s[1], s[2], s[3]) ;

}